<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:TabletopRolePlayingCharacterManager">
	<DataTemplate x:Key="Skill">
		<StackPanel Orientation="Vertical"
							Margin="4, 0, 4, 0"
							BorderBrush="DarkGray"
							BorderThickness="2">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="{Binding Path=Name, Mode=TwoWay}" VerticalAlignment="Center"
							   MinWidth="112"/>

			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="{Binding Path=Bonus, Mode=TwoWay}"
								   Margin="8"
								   FontSize="18"/>
				<CheckBox IsChecked="{Binding Path=IsProficient, Mode=TwoWay}" MinWidth="32"/>
				<TextBlock Text="{Binding Path=MainStat, Mode=TwoWay}" HorizontalAlignment="Right" Foreground="#FF7E7E7E" Margin="4,0,0,0"
								  />
			</StackPanel>
		</StackPanel>
	</DataTemplate>
	
	<DataTemplate x:Key="ProficiencyTemplate">
		<StackPanel>
			<TextBlock Text="{Binding Name}"/>
		</StackPanel>
	</DataTemplate>

	<DataTemplate x:Key="Item">
		<StackPanel Margin="4"
						MinWidth="150">
			<TextBox Text="{Binding Path=Name, Mode=TwoWay}" Margin="0, 4"/>
			<TextBox Text="{Binding Path=Description, Mode=TwoWay}"
							  TextWrapping="Wrap"
						MinHeight="48"
						 AcceptsReturn="True"
						 />
		</StackPanel>

	</DataTemplate>
	<DataTemplate x:Key="Weapon">
		<StackPanel Margin="4">
			<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
				<TextBox Text="{Binding Path=Name, Mode=TwoWay}"
							 Margin="0,0, 4, 0"
						 MinWidth="96"/>
				<TextBox Text="{Binding Path=Damage, Mode=TwoWay}"
							 Margin="0, 0, 4, 0"
						 PlaceholderText="1d6, 1d8, etc."/>
				<TextBox Text="{Binding AttackBonus, Mode=TwoWay}"
							 PlaceholderText="Attack Bonus"/>


			</StackPanel>
			<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
				<ComboBox ItemsSource="{Binding MainStat}"
							  SelectedIndex="{Binding SelectedMainStat, Mode=TwoWay}"
							  Margin="0, 0, 4, 0"/>
				<ComboBox ItemsSource="{Binding WeaponTypes}"
						  SelectedIndex="{Binding SelectedWeaponType, Mode=TwoWay}"/>
			</StackPanel>
			<TextBox Text="{Binding Description, Mode=TwoWay}"
						 PlaceholderText="Description"
						 AcceptsReturn="True"
						 MinHeight="48"
						 Margin="0, 4, 0, 0"
						 TextWrapping="Wrap"/>
		</StackPanel>
	</DataTemplate>
	<DataTemplate x:Key="Spell">
		<StackPanel Orientation="Horizontal"
						Margin="0, 4" BorderThickness="1" BorderBrush="#FF666666">
			<StackPanel>
				<TextBox Text="{Binding Path=Name, Mode=TwoWay}"
							 MinWidth="96"
							 Margin="0, 0, 0, 4"/>
				<ComboBox ItemsSource="{Binding Path=Levels}"
							  SelectedIndex="{Binding SelectedLevel}"
							 MinWidth="96"/>
			</StackPanel>
			<StackPanel Margin="4, 0">
				<StackPanel Orientation="Horizontal">
					<CheckBox Content="V"
							  IsChecked="{Binding HasVerbalComponent}"
							  MinWidth="40"/>
					<CheckBox Content="S"
							  IsChecked="{Binding HasSomaticComponent, Mode=TwoWay}"
							  MinWidth="40"/>
				</StackPanel>
				<TextBox Text="{Binding MaterialComponent, Mode=TwoWay}"
							 PlaceholderText="Material"/>
			</StackPanel>
			<TextBox Text="{Binding Path=Description, Mode=TwoWay}"
							  TextWrapping="Wrap"
							 MinWidth="120"
						 AcceptsReturn="True"/>
		</StackPanel>
	</DataTemplate>
	<DataTemplate x:Key="CharacterTrait">
		<TextBlock Text="{Binding Description, Mode=TwoWay}"
					   MinHeight="72"/>
	</DataTemplate>
</ResourceDictionary>

